x=c(1:6); #Crea un rango del 1 al 6
dunif(x, min=1, max=6); #Densidad de probabilidad
punif(x, min=1, max=6); #Suma de probabilidades
qunif(0.2, min=0, max=1); #Funcion inversa
runif(10,min=0, max=1);#Genera un determinado numero de numeros
                        #aleatorios dentro de la distribucion uniforme
dbinom();#Para la distribuicon binomial
dexp();#Distribucion exponencial
dnorm();#Distribucion Normal
dpois();#Distribucion Poison